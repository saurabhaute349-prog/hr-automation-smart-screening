# hr-automation-smart-screening
AI-powered HR automation workflow built with n8n. Automatically reads job emails, extracts CVs, classifies applications, scores candidates, saves data to Google Sheets, generates interview questions, and sends auto-emails.
